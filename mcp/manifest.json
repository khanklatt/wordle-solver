{
  "name": "wordle_solver",
  "version": "1.0.0",
  "description": "Suggests Wordle guesses and filters candidates based on feedback",
  "tools": [
    {
      "name": "suggest_guess",
      "description": "Given feedback, suggest the next Wordle guess",
      "input_schema": {
        "type": "object",
        "properties": {
          "guess": {"type": "string"},
          "greens": {"type": "string"},
          "yellows": {"type": "string"},
          "greys": {"type": "array", "items": {"type": "string"}}
        },
        "required": ["guess", "greens", "yellows", "greys"]
      }
    }
  ]
}
